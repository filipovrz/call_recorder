# Block direct HTTP access to the APK.
# Downloads must go through /download.php so they are counted.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
